#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
  int method(vector<int> &colors, int k) { return 0; }
};

int main() {
  Solution solution;
  // vector<int> nums = {1, 3, 8, 48, 10};

  // ----- integer -----
  // int result = solution.method();
  // cout << result << endl;

  // ----- vector of integers -----
  // vector<int> result = solution.method();
  // for (int num : result) {
  //     cout << num << " ";
  // }
  // cout << endl;

  // ----- vector of vector of integers -----
  // vector<vector<int>> result = solution.method();
  // for (const auto &row : result) {
  //     for (int num : row) {
  //         cout << num << " ";
  //     }
  //     cout << endl;
  // }

  // ----- boolean output (prints 1 for true, 0 for false) -----
  // bool result = solution.method();
  // cout << (result ? 1 : 0) << endl;

  // ----- string -----
  // string result = solution.method();
  // cout << result << endl;

  return 0;
}
